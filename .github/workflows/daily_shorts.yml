name: Generate Daily Hindi Shorts

on:
  workflow_dispatch: # YEH LINE HI RUN KA BUTTON LAYEGI!
  schedule:
    - cron: '45 1 * * *' # Roz subah automatic chalega (IST ke hisab se subah 7:15)

jobs:
  build_and_run:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        pip install feedparser requests
        pip install -r requirements.txt

    - name: Fetch Latest RSS Topic
      run: python fetch_rss.py

    - name: Run MoneyPrinterTurbo
      env:
        PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
        YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
      run: |
        python -m app.main --config config.json

